# V57 Run Summary: Unexhausted Method Frontier

Status: **four-active-hour target met; exploration complete at a clean point**

## Measured Runtime

- Block start: `2026-08-29T21:54:47Z`.
- Credited interval 1: `2026-08-29T21:54:47Z` to
  `2026-08-30T01:10:17Z` = `03:15:30`.
- Credited interval 2: `2026-08-30T11:21:03Z` to
  `2026-08-30T12:12:12Z` = `00:51:09`.
- Cumulative credited active time: `04:06:39`.
- Wall-clock span: `14:17:25`.
- Excluded scheduler/resume gaps: `10:10:46`. Brief work inside ambiguous
  intervals was conservatively not credited.
- Four-hour active target met: **yes**.

## Scope And Outcome

V57 audited `30` analytical method families and executed `49` substantive
method items. It did not discover a cure, a better treatment, an
intervention-grade target, a progression biomarker, or an externally validated
monitoring biomarker. Public-data discovery remains closed under V41's
exhaustion verdict.

The broad held-data search was informative mainly through corrected nulls.
Environment invariance, selective conformal prediction, hierarchical pooling,
tensor interactions, compositional shifts, optimal transport, multivariate
geometry, neighborhood abundance, topology, and formal cross-environment
replicability did not clear their frozen gates. Complexity again did not create
a transportable signal that the scalar had missed.

## New Methods That Earned Continued Use

1. **Privacy-preserving same-estimand validation.** A pinned no-egress
   code-to-data package executes the immutable V22 harness behind a data-owner
   boundary and returns only hash-attested aggregate results. It passed leak,
   tamper, and schema tests on labeled synthetic fixtures.
2. **Dependence-aware federated accumulation.** Independent and declared-
   cluster paths now preserve effect size and uncertainty, reject hidden schema
   drift, and use calibrated anytime-valid evidence. Correlated-site simulation
   showed why nominal site count cannot substitute for independent evidence.
3. **Replication as a separate claim.** A complete fixed four-site
   partial-conjunction gate requires effects in at least two sites. All `6/6`
   fixtures passed, and `4.5` million null families had maximum false
   replication `0.049464`. One exceptional site alone explicitly failed
   replication (`p=0.60`).
4. **Direction-resolving human functional program.** The best progression-
   relevant experimental route is bidirectional CRISPRi/a in donor-replicated
   human iPSC microglia, followed only when justified by crossed-donor 3D glial
   confirmation. Efficacy, viability, neural-support/myelin, guide, context,
   batch, negative-control, variance, and model-escalation gates are specified.
   This is an experimental proposal, not biological evidence or a target
   nomination.
5. **Conditional combinatorial and causal-design tools.** Descriptor-based
   D-optimal pair selection is useful only when descriptors predict held-out
   interactions better than random coverage. Trial/IPD routing now fails closed
   when time zero, outcomes, modifiers, or overlap cannot support the proposed
   progression estimand.
6. **V22 integrity suite.** Specification, influence, competitive-null,
   missing-label, label-integrity, batch, shift, and measurement-architecture
   diagnostics are ready around the locked rule without changing it.

## Important Negative Method Results

- The V22 association was detectable in pooled held data but not stable in the
  worst environment; cross-environment recurrence remains unestablished.
- Module measurement architecture was not invariant across environments.
- A flexible successor had no observed gain, but uncertainty was too wide to
  prove non-superiority.
- Naively multiplying correlated site evidence inflated null crossing to
  `0.08214`. Three high-correlation evidence clusters were underpowered; four
  were the first passing point only under the fixed synthetic generator, not a
  universal cohort minimum.
- The first multifidelity safety gate and a naive negative-control rule failed;
  both failures remain visible rather than being silently replaced by their
  later calibrated extensions.

## Verification And Tooling

- Final V57 claim/artifact regression: `92/92` passing.
- Provenance gate: pass; external method and model records remain explicitly
  not project-grounded.
- Structural-prediction gate: pass; no structural prediction became grounded
  evidence.
- Sparse grounded retrieval index: `1035` unique documents, `0` paths from
  `knowledge_external/`; V57 synthesis and replication-boundary queries return
  their intended artifacts first.
- Large-file/tmp guard: pass.
- Git: iteration commits pushed successfully to `origin/main`.

Claude and Gemini were used only as generic, segregated method critics. Their
concrete suggestions were tested: leave-one-donor checks added no demonstrated
protection, while a naive negative-control rule failed and a separately frozen
finite-sample correction passed. RPT was smoke-tested but not used
substantively because no defensible tabular prediction input existed. Model
spend was not exposed by the client.

OpenGWAS access returned `401` because the configured token was expired. V57
routed around all OpenGWAS-dependent work and made no genetics null claim from
failed authentication.

## Honest Next Step

The highest-value action is no longer another computational search. Offer the
pinned code-to-data V22 package to eligible independent data owners and preserve
the fixed four-site replication claim. In parallel, acquire blinded pilot
variance for the bidirectional donor-replicated human microglia assay before
nominating any gene or combination. These two routes address the actual
remaining bottlenecks: independent validation data and intervention direction.

Stop reason: the four-active-hour target was met and all current work was left
committed, reproducible, and resumable.
