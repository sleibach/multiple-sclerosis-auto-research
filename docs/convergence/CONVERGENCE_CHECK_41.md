# Convergence Check 41

Timestamp: 2026-05-27 18:02 CEST

## Trigger

Continuation after usage-limit interruption. Reconciled the residual
targetability audit and re-ran Wave81 perturbation-first rescue with stricter
support definitions.

## What Each Track Now Believes

Expression-derived targetability track:

- `CD58` remains a real genetics/response-context signal, but not a promotable
  target.
- `P4HB`, `SPNS1`, and `SEL1L3` remain closed as therapeutic nodes.
- The stricter residual audit found only isolated Sjogren residual contexts for
  `P4HB` and `SPNS1`; neither has MS anchoring or intervention direction.

Perturbation/foundation-model track:

- Wave81 initially risked proxy-satisficing: model support was table presence,
  not positive prediction.
- After stricter support gates, no candidate reopens.
- `SP140`, `RGS14`, and `STAT4` are explicit false-positive controls:
  - `SP140`: Geneformer support contexts `0`; efferocytosis call `UNRESOLVED`.
  - `RGS14`: Geneformer support contexts `0`; no direct perturbation.
  - `STAT4`: direct perturbation `null_or_wrong_direction`; no positive model
    support.

Treatment-response track:

- IBD response signals remain nominal and weak after FDR correction.
- RA anti-TNF response signals are useful as context markers, but have not
  supplied a novel intervention point.

## Agreement

- The lipid-lysosomal/myeloid module remains a recurring disease-state pattern.
- The module does not yet imply a target: every candidate so far fails at least
  one hard translational gate: direction, druggability, causal anchoring,
  positive perturbation, or novelty.
- Table presence is not evidence. Every future branch must require the source
  table's own positive support statistic.

## Disagreement / Remaining Uncertainty

- `DAB2`, `CD9`, `PARK7`, and `PSAP` are the least-bad parked rows after the
  stricter perturbation-first audit, but their evidence channels are incomplete:
  - `DAB2` and `CD9`: direct efferocytosis signal plus MS expression, but no
    genetics/modality/foundation support.
  - `PARK7`: positive model support and a modality channel, but no MS anchor.
  - `PSAP`: model support and MS expression, but no clear druggability or
    cross-autoimmune breadth.

## Next Forcing Question

Can any parked perturbation candidate be converted into a real intervention
claim through a credible modality and cross-disease support, or are they only
mechanistic readouts?

## Decision

Open Wave82 intervention-route stress test. Dispatch sidecars for:

- hostile translational feasibility/prior-art audit;
- cross-disease evidence stress test.

No V3 finding is ready.
