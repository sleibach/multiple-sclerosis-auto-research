# Convergence Check 42

Timestamp: 2026-05-27 18:13 CEST

## Trigger

Wave82 completed with two sidecars and local public-source/druggability audit.

## What Each Track Now Believes

Perturbation/readout track:

- `DAB2` and `CD9` have the clearest direct efferocytosis-screen flags plus MS
  expression, but the perturbation statistics are weak after adjustment and
  neither has cross-disease state breadth or target-resolution genetics.
- `PARK7`, `PSAP`, `HEXA`, and `HEXB` are biology probes, not intervention
  points.

Genetics/breadth track:

- `SP140`, `STAT4`, and `RGS14` remain evidence-bearing genetics nodes.
- `SP140` has the most module/state breadth but lacks positive strict
  perturbation/foundation-model support and remains prior-branch blocked.
- `STAT4` has the strongest cross-autoimmune genetics but is a broad
  transcription-factor/JAK-STAT axis problem, not a selective target.
- `RGS14` has MS/Crohn/psoriasis genetic target resolution but no module-state
  breadth or direct perturbation.

Druggability/prior-art track:

- `LYN`, `HEXA/HEXB`, `PARK7`, `PSAP`, `SP140`, and `STAT4` have some
  modality or chemical precedent.
- That does not rescue them: directionality, safety, prior art, or lack of
  disease-specific anchoring blocks all.

## Agreement

- No Wave82 candidate is a V3 finding.
- The module continues to produce strong readouts but weak intervention points.
- The strongest genetic nodes and strongest state nodes are not the same as the
  most tractable drug targets.

## Disagreement / Remaining Uncertainty

- `SP140` remains scientifically compelling but translationally stuck.
- `RGS14` could represent a missed MS/Crohn/psoriasis regulatory axis, but
  current data do not place it in the lipid-lysosomal myeloid module or provide
  a perturbable route.
- `LYN` is tractable chemically, but the safety/direction problem is severe
  enough that a simple inhibition claim would likely be wrong.

## Next Forcing Question

Is there a target-resolved, druggable genetics node outside the Wave81 parked
perturbation set that survived because it failed no hard safety/direction gate,
or have prior scans already closed that space?

## Decision

Close parked perturbation rescue. Open Wave83: genetics-first/druggable-survivor
sweep across all Wave62/Wave55 target-resolution rows, with explicit prior
closure checks.
