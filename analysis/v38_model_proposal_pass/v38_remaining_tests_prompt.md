You are a proposal lens, not an evidence source. The project will not treat
your output as fact. Propose only concrete tests that can be grounded using
existing committed tables/reports in this repository.

Context:

- V38 has already completed:
  - adversarial inversion of the bounded V22/V23 monitoring scalar;
  - failure-structure meta-analysis;
  - RPT structural mining;
  - exclusion/non-replication ledger;
  - control-system reframing;
  - coupled APC architecture global-tone inversion;
  - MS-UC LDSC backdrop inversion;
  - direction/modality prefilter;
  - layer-transfer map inversion;
  - V36 exploratory fragility map.
- Key results so far:
  - V22 scalar survives as bounded, small-n, immune-tone-conditioned, not
    MS-calibrated, pending Gafson/DMF validation.
  - V26 coupled APC architecture is tone-loaded but not pure tone artifact; it
    is not a better predictor than the scalar.
  - MS-UC rg survives recorded LDSC inversion; MHC sensitivity caveat remains.
  - Layer-transfer map is axis-specific, not just narrative disease similarity.
  - Failures cluster around context/axis dependence, evidence-resolution limits,
    direction/modality mismatch, multiplicity/overfit, and data modality gaps.

Task:

Propose 3 unconventional but table-groundable V38 tests that remain worth doing.
For each:

1. Name the hypothesis/inversion.
2. Name the exact committed artifact type likely needed (examples: V37 scored
   findings TSV, V38 failure table, V36 fragility outputs, V8-V12 matrices,
   V26 module matrices, V32 confounder table).
3. State the concrete computable test.
4. State what result would strengthen, weaken, or demote a current conclusion.

Constraints:

- No new data.
- No locked rule edits.
- No model opinions as evidence.
- Prefer tests that produce a small table and a clear V37-to-V38 delta.
