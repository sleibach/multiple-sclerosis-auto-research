# V53 Cell-State De-overlap Sensitivity

Verdict: **CELL_STATE_HLA_MIF_EDGE_FAILS_DISJOINT_READOUT_GATE_GLOBAL_STATUS_REQUIRES_REASSESSMENT**.

Five held h5ad files were read once to rebuild all `12` donor-level
contexts under original and globally disjoint module definitions. Original donor
scores and the V26 matrix reproduce to maximum errors `9.71e-17` and
`8.33e-17`.

HLA-II/APC versus receptor-state rho changes from
`0.832` to
`0.175` with six-pair BH
`q=0.5819`. This sensitivity alone does not
change the global architecture; remaining modalities require separate rebuilds.
