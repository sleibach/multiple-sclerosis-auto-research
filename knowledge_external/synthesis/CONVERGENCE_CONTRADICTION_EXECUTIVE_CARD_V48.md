# V48 Convergence/Contradiction Executive Card

Status: synthesis/navigation only. This card summarizes V48 external-context relationships; it does not validate external claims or change grounded findings.

## Headline

- V48 currently has `2` convergence rows and `0` contradiction rows.
- The decision-relevant rows collapse to `1` canonical external source cluster, so row count must not be treated as independent-source count.
- The high-priority uncovered V37 sourcing backlog has `11` rows.
- Governance preflight status: `PASS`.

## Metrics

| section | metric | value | boundary |
|---|---|---:|---|
| `relationship_counts` | V48 matrix rows | 12 | External relationship rows are context, not evidence. |
| `relationship_counts` | convergence rows | 2 | Convergence corroborates context; grounded artifacts remain evidence. |
| `relationship_counts` | contradiction rows | 0 | Contradictions would flag future grounding, not override grounded findings. |
| `source_independence` | decision canonical source clusters | 1 | Same canonical source cluster is not multiple independent corroborations. |
| `source_independence` | domains with convergence | 1 | Domain concentration limits external-source independence. |
| `coverage_gaps` | V37 uncovered priority rows | 20 | Sourcing priority is not validation or convergence. |
| `coverage_gaps` | high-priority V37 sourcing gaps | 11 | Only same-definition external sources should be added. |
| `governance` | preflight checks | 60 | Preflight checks provenance/navigation controls only. |
| `governance` | preflight failures | 0 | Zero failures means the segregation controls passed. |

## Current Decision-Relevant Rows

| finding | evidence grade | relationship | source | boundary |
|---|---|---|---|---|
| Layer-specific autoimmune transfer-validity map | `supported` | `converges` | https://www.nature.com/articles/s41467-021-25768-0 | Grounded artifact remains the evidence; external source is context/corroboration or tension flag only. |
| MS-UC is strongest tested genome-wide genetics comparator | `robust` | `converges` | https://www.nature.com/articles/s41467-021-25768-0 | Grounded artifact remains the evidence; external source is context/corroboration or tension flag only. |

## Top High-Priority Sourcing Gaps

| rank | finding | score | safe source requirement |
|---:|---|---:|---|
| 1 | Mucosal IBD early IFN/APC downshift validates while baseline fallback fails | 18 | Require a source directly overlapping the grounded finding before adding a V48 relationship row. |
| 2 | UC genetics vs treatment-response layer split | 17 | Require a source that addresses the same direction, locus, failure mode, or definition; generic disease context is insufficient. |
| 3 | First-principles druggability discipline changed target interpretation | 16 | Require a method-specific source about the same procedure or governance issue; broad disease biology is not sufficient. |
| 4 | Locked V7 general cross-disease baseline fallback killed | 16 | Require a source that addresses the same direction, locus, failure mode, or definition; generic disease context is insufficient. |
| 5 | Tool-robust but simple V22 scalar | 15 | Require a method-specific source about the same procedure or governance issue; broad disease biology is not sufficient. |

## Interpretation Boundary

- External convergence is corroborating context only; project artifacts remain the evidence.
- Current convergence is source-cluster concentrated and should be described that way.
- Current zero contradictions means no imported external record directly conflicts with a grounded finding under V48 rules; it is not proof that no contradiction exists in the wider literature.
- Sourcing gaps are a future external-intake plan, not findings.
