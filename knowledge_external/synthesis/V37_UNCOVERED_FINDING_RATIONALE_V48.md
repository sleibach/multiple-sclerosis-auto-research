# V37 Uncovered Finding Rationale V48

Status: synthesis/navigation only. Absence of a V48 external relationship row is not a biological negative and not external contradiction.

- uncovered V37 findings: `20`

## Rationale Counts

| rationale class | count |
|---|---:|
| `avoid_false_corroboration` | 1 |
| `method_specific_external_context_absent` | 3 |
| `no_relevant_external_record_imported` | 1 |
| `targeted_external_record_needed` | 15 |

## Findings

| finding | category | evidence | rationale class | rationale | next action |
|---|---|---|---|---|---|
| Tool-robust but simple V22 scalar | `methodological` | `supported` | `method_specific_external_context_absent` | The item is a project-method or governance result; external literature rarely maps one-to-one to the exact project procedure. | Add only method-specific external context, not broad biological context. |
| Mucosal IBD early IFN/APC downshift validates while baseline fallback fails | `positive_supported` | `supported` | `no_relevant_external_record_imported` | No imported external record overlaps the finding closely enough to assert convergence or contradiction under V47/V48 provenance rules. | Future V47-style intake can add a sourced external record if it directly overlaps the finding. |
| IFN-beta HLA-II/CD74 branch | `positive_supported` | `provisional` | `avoid_false_corroboration` | The project finding is not robust enough to benefit from broad external context; generic literature agreement would risk overstating a provisional result. | Queue a future external-verifiable task only if a source points to a concrete dataset or predefined test. |
| MHC overlap is distinct-signal, not simple shared biology | `decoupling_negative` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| UC genetics vs treatment-response layer split | `decoupling_negative` | `supported` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Crohn downstream IFN/APC convergence exceeds genetic proximity | `decoupling_negative` | `supported` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| RA pregnancy comparator but blood APC treatment-response nontransfer | `decoupling_negative` | `supported` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Sjogren antigen-presentation but not lysosomal/APC lesion-rim transfer | `decoupling_negative` | `supported` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| No load-bearing invariant found in V26 | `decoupling_negative` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Locked V7 general cross-disease baseline fallback killed | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| GPR25 demoted from protected favorite | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| NAMPT/eNAMPT not reactivated as target | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| ZFP36L1 chr14 parked | `kills_closed` | `provisional` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| REL/PUS10/USP34 chr2 closed | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| EBV/IFN APC imprint downgraded by specificity control | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Complement/lipid progressive axis downgraded | `kills_closed` | `negative-established` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Lysosomal APC bottleneck not proven | `kills_closed` | `provisional` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Metabolic/sterol setpoint is context/confounder axis, not intervention-grade | `kills_closed` | `provisional` | `targeted_external_record_needed` | This is a project-specific negative, kill, or decoupling result; V48 should not force a generic external context row without a source that addresses the same direction/definition. | Only add external context if a source directly discusses the same locus/lead/failure mode and can be classed without changing the grounded result. |
| Multi-lineage and RPT lenses add prioritization, not evidence | `methodological` | `supported` | `method_specific_external_context_absent` | The item is a project-method or governance result; external literature rarely maps one-to-one to the exact project procedure. | Add only method-specific external context, not broad biological context. |
| First-principles druggability discipline changed target interpretation | `methodological` | `supported` | `method_specific_external_context_absent` | The item is a project-method or governance result; external literature rarely maps one-to-one to the exact project procedure. | Add only method-specific external context, not broad biological context. |

## Interpretation

- This table explains non-coverage; it does not demote, validate, or contradict any finding.
- External context should be added only when source overlap is specific enough to preserve provenance discipline.
- Grounded project artifacts remain the evidence for every V37 item.
