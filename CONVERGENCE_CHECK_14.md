# Convergence Check 14

Timestamp: 2026-05-27 09:05 UTC

## Active-Time Status

The usage-limit wait gap still does not count as work time. Based on
`TIME_ACCOUNTING_V3.md`, active work is above ten hours but still below the
twelve-hour floor. `MILESTONE_6.md`, `MILESTONE_6_MISS.md`, and any exhaustion
document are therefore not yet due.

## What The Tracks Now Believe

Corrected perturbation track:

- Wave35 fixed the Ensembl mapping artifact by adding exact-symbol MyGene.info
  fallback mapping.
- Corrected module coverage is now high in the Ensembl-indexed perturbation
  datasets.
- The corrected strict controller gate still returns `0` controller-like
  perturbation contrasts across 10 datasets and 29 contrasts.

Gene-level rescue track:

- Wave36-A found permissive submodule and gene-rescue-shaped contexts, but no
  promotion-ready target route.
- `RXR/LXR` remains one-dataset-only; `LIPA`, `GPNMB`, `IL10`, `MERTK/TAM`, and
  `TREM2` fail replication, direction, stress/fibrosis, or route feasibility.

Direct efferocytosis CRISPR track:

- Wave37 added `GSE212008`, a primary murine BMDM pooled CRISPR screen for
  efferocytosis state sorting.
- Canonical resolution candidates did not rescue the route.
- `FCGRT` surfaced as a superficially tractable KO-enhancer, but Wave38 rejected
  it after disease-state direction, MS-anchor, and prior-art checks.

Hostile critique:

- Wave36-B argues that active discovery inside the resolution/efferocytosis
  branch should stop.
- The branch remains useful as a biomarker/readout panel and comparator, not as
  the current therapeutic target-discovery lane.

## Agreement

All active tracks now agree that the downstream resolution/efferocytosis route
does not currently yield a defensible V3 intervention point under the required
gates. The failure is not from a single weak test: it persists after mapping
repair, module-level perturbation, gene-level perturbation, direct CRISPR screen
triage, disease-state integration, druggability checks, and hostile critique.

## Disagreement Or Residual Uncertainty

- A true pro-resolution mechanism may still exist, but the available public
  perturbation data do not identify a target with enough specificity,
  replication, and safety guardrails.
- The branch may be strongest as an assay/readout system for future wet-lab
  screening rather than as a computational target nomination.

## Next Forcing Question

Can an accessible surface, secreted, or extracellular intervention point outside
the resolution/efferocytosis branch survive the same cross-disease breadth,
MS-anchor, druggability, novelty, and directionality gates that eliminated the
intracellular and downstream-resolution candidates?

The next analysis will be an accessibility-first rescue over broad
cross-autoimmune recurrence, explicitly excluding already-demoted candidates
where appropriate.
